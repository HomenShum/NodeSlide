# NODESLIDE-NOTES-CONTROLS-01

**Re your request:** execute individual repository readiness, including visual, responsive and interaction standards. A presenter must be able to edit what they will say, move between slides and adjust zoom without one control covering another. Current merged817 screenshots show navigation and zoom over the notes textarea; at320 px the next-slide button is obstructed.

This is a bounded UI patch. Root viewed the actual current390 normal,1440 normal and390 doubled-text labelled3px before PNGs in E6g-nodeslide-notes-before-01. Ten captured states cover six normal widths plus390/1440 doubled text and two collapsed-note states. Browser console/page errors and external requests are zero. Full HTML and the synthetic capability remain private. The mistaken `.ns-slide-stage` region selector is missing in those JSON files; notes/control bounds and actual pixels remain valid. Future captures use the actual `.ns-canvas-stage` selector and ignore zero-size hidden controls for hit checks.

The causal chain is: the note editor is covered because bottom controls overlay it; they overlay it because they are absolutely positioned relative to the whole panel; notes occupy a newly added normal grid row but do not change those offsets; V3 pins both controls to bottom0 while preserving the old zoom left50%/translate transform; phone styles re-enable navigation without allocating it space. The shared layout has no row that owns the controls' height and wrapping. Increasing z-index or guessing a note-height offset would preserve this cause.

Root accepts the independent planner's current source/geometry diagnosis. Change only SlideCanvas.tsx, nodeslide.css and nodeslideV3.css, plus a truthful HANDOFF.md entry after proof. Group the existing pager and zoom into one ordinary `.ns-canvas-controls` row immediately after the slide viewport. Keep notes as the next in-flow row. Use flex wrapping and existing spacing to keep both groups reachable at narrow and enlarged widths. Remove their old absolute bottom/left/right/translate positioning at the existing CSS owners. Keep all handlers, callbacks, slide-count/zoom semantics, note keys/save-on-blur, runtime/provider/export policy and public APIs unchanged. No dependencies, config options, timing changes or new state. Do not expand into the observed header/banner/slide-text clipping or general design redesign.

Before editing, preserve exact owner bytes and bind the current head/index/refs. After editing, compare the same ten states plus320 doubled text, inspect actual PNGs, and require positive-size control rectangles, label bounds and all interaction target hit points to be unobstructed within the viewport. Notes must not intersect the controls; pager and zoom may wrap into separate rows. Exercise native Tab/Enter notes open/close, note edit/save/reload, previous/next boundaries, zoom minus/plus/Fit and repeated note revisions. Save and reopen current JSON/PPTX with a unique later note; prior files remain exact. Use only the existing isolated synthetic backend. Compare old CSS/structure in one bounded knockout if needed to prove geometry checks detect the recorded defect, restoring exact current bytes afterwards.

Run existing affected interaction/keyboard component scenarios and one ordinary repository check after the source change. Preserve any Windows lazy-loader failure; no timeout increase or retry-to-green. A current failing full check remains a hold even if targeted checks pass. Independent judgment must inspect raw source, actual pixels, native actions and file payloads before publication and normal shared CI. The full portfolio and repository grade stay open; scoped geometry success cannot certify device, accessibility, performance, production, provider or native Office behavior.
