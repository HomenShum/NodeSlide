import fs from 'node:fs/promises';import path from 'node:path';import{createRequire}from'node:module';import{fileURLToPath}from'node:url';
const P=path.dirname(fileURLToPath(import.meta.url));const req=createRequire('D:/VSCode Projects/cafecorner_nodebench/nodebench_ai4/.portfolio-worktrees/nodeslide-current-consumer-20260905/package.json');const{chromium}=req('playwright');const{expect}=req('playwright/test');
const base='D:/ProjectRecovery/NodeSlide first run 20260905/private-bootstrap-02';const O=path.join(P,'E6g-nodeslide-counts-before');await fs.mkdir(O);const priv=path.join(base,'counts-before');await fs.mkdir(priv);
const browser=await chromium.launch();const report={proof:'NODESLIDE-DECK-CI-DETAIL-01',at:new Date().toISOString(),cells:[],errors:[],blockedExternal:[],grade:null};
async function capture(page,name){await page.evaluate(()=>document.fonts.ready);await page.screenshot({path:path.join(O,name+'.png')});await fs.writeFile(path.join(priv,name+'.html'),await page.content());const dom=await page.evaluate(()=>({text:document.body.innerText,viewport:{width:innerWidth,height:innerHeight},overflow:document.documentElement.scrollWidth-innerWidth,details:[...document.querySelectorAll('[data-testid="deck-ci-details"]')].map(e=>({text:e.textContent,version:e.getAttribute('data-deck-version'),box:(()=>{const r=e.getBoundingClientRect();return{x:r.x,y:r.y,width:r.width,height:r.height}})()})),states:[...document.querySelectorAll('.ns-deck-ci-status')].map(e=>({state:e.getAttribute('data-state'),text:e.textContent,animation:getComputedStyle(e.querySelector('.ns-deck-ci-status__dot')).animationName})),focused:document.activeElement?.getAttribute('aria-label')}));await fs.writeFile(path.join(O,name+'.json'),JSON.stringify(dom,null,2)+'\n');return dom;}
try{for(const viewport of [{width:390,height:844},{width:1440,height:960}])for(const subject of [{kind:'fail',folder:'browser-first-use-01'}]){
 const root=path.join(base,subject.folder);const{url}=JSON.parse(await fs.readFile(path.join(root,'deck-location.json'),'utf8'));const context=await browser.newContext({viewport,storageState:path.join(root,'storage-state.json'),serviceWorkers:'block'});await context.route('**/*',r=>{const u=new URL(r.request().url());if(['http:','https:'].includes(u.protocol)&&u.hostname!=='127.0.0.1'){report.blockedExternal.push(u.origin);return r.abort();}return r.continue();});const page=await context.newPage();page.on('pageerror',e=>report.errors.push(e.message));page.on('console',m=>{if(m.type()==='error')report.errors.push(m.text());});
 for(let round=0;round<1;round++){
  await page.goto(url,{waitUntil:'domcontentloaded'});await page.getByTestId('slide-canvas').waitFor();await page.getByTestId('inspector').waitFor({state:'attached'});
  if(!await page.getByTestId('inspector-tab-ai').isVisible())await page.getByRole('button',{name:'Open inspector',exact:true}).click();
  await page.getByTestId('inspector-tab-ai').click();const button=page.getByRole('button',{name:/Deck CI .*Open Trace/});await expect(button).toContainText(subject.kind==='fail'?'Failed':'Passed');
  // Reach the real failure entry with native Tab navigation; activate with Enter.
  await page.getByTestId('inspector-tab-ai').focus();let tabs=0;while(!(await button.evaluate(e=>e===document.activeElement))&&tabs<60){await page.keyboard.press('Tab');tabs++;}expect(tabs).toBeLessThan(60);await page.keyboard.press('Enter');
  const details=page.getByTestId('deck-ci-details');await expect(details.locator('.ns-deck-ci-status')).toHaveAttribute('data-state',subject.kind);await expect(details).toHaveAttribute('data-deck-version','3');
  if(subject.kind==='fail'){await expect(details).toContainText('Blocker');await expect(details).toContainText(/near-duplicate|similar|composition/i);}else await expect(details).toContainText('No blockers or warnings reported');
  await expect(page.locator('body')).not.toContainText('publish ready');await expect(page.getByTestId('validation-status')).toContainText('Core validation v3 passed. Review Deck CI.');
  if(round===0){const dom=await capture(page,`${viewport.width}-${subject.kind}-normal`);expect(dom.overflow).toBe(0);expect(dom.states.every(s=>s.animation==='none')).toBe(true);report.cells.push({viewport,subject:subject.kind,round,tabs,overflow:dom.overflow});}
  else report.cells.push({viewport,subject:subject.kind,round,tabs});
 }
 // Explicitly labeled component-text scaling, not an OS/browser zoom certificate.
 await page.evaluate(()=>{const nodes=[...document.querySelectorAll('[data-testid="inspector"] *')];const sizes=nodes.map(e=>[e,parseFloat(getComputedStyle(e).fontSize)]);for(const[e,size]of sizes)if(size>0)e.style.setProperty('font-size',`${size*2}px`,'important');});
 await page.evaluate(()=>{const e=document.querySelector('[data-testid="deck-ci-details"] .ns-deck-ci-status__counts');const r=e.getBoundingClientRect();e.style.outline='3px solid #d20c39';const label=document.createElement('div');label.textContent='C — Enlarged counts clipped';Object.assign(label.style,{position:'fixed',top:Math.max(0,r.top-25)+'px',left:r.left+'px',font:'12px sans-serif',color:'white',background:'#d20c39',zIndex:'999999'});document.body.append(label);});
 const scaled=await capture(page,`${viewport.width}-${subject.kind}-text200`);report.cells.push({viewport,subject:subject.kind,textPercent:200,overflow:scaled.overflow,scope:'Computed inspector component text sizes doubled; no whole-page zoom claim'});await context.close();
 }expect(report.errors).toEqual([]);expect(report.blockedExternal).toEqual([]);report.status='PASS_NORMAL_ACTUAL_UI_WITH_TEXT200_OBSERVATIONS';
}catch(e){report.status='FAILED';report.failure=String(e.stack??e);process.exitCode=1;}finally{await browser.close();await fs.writeFile(path.join(O,'report.json'),JSON.stringify(report,null,2)+'\n');console.log(JSON.stringify(report));}
