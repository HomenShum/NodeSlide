---
name: gemma-4-free-artifact-adapter
description: Apply evidence-backed safeguards when Gemma 4 26B Free plans or generates a bounded NodeSlide artifact. Use for free-router artifact planning, source-bound visual operations, or zero-cost candidate generation.
---

# Gemma 4 Free Artifact Adapter

The Artifact Atlas v1 planning run produced valid structured plans for all 24 assigned candidates across 12 artifact types and two visual directions. Treat the role as provisional until the rendered receipts and a skill-on/skill-off ablation pass.

## Contract

1. Verify the requested OpenRouter free route is live and record its exact model id. Never accept a generic fallback as Gemma evidence.
2. Assign one bounded artifact, its required operations, source identifiers, and an exact response schema. Do not assign whole-deck taste or final selection.
3. Keep batches at three candidates or fewer and cap the context to the supplied fixture evidence.
4. Require the plan to name the artifact type, source-bound operations, visual direction, takeaway, annotation, and an honest PPTX fallback.
5. Send the valid plan to deterministic semantic builders for browser and editable PowerPoint construction.
6. Run independent evidence, collision, export, and editability critics. Gemma does not certify its own result.
7. Permit one bounded repair attempt for schema or provider failure, then fail explicitly with route telemetry.

## Completion checks

- The route receipt identifies `google/gemma-4-26b-a4b-it:free`.
- Every operation references an allowed source id and required operation id.
- No unsupported claim or static fallback is hidden as a native artifact.
- Browser and PPTX renders exist and pass independent critics.
- Cost and latency are recorded; zero cost is not treated as quality evidence.

Do not promote Gemma into a full-deck or final-taste role from artifact-planning evidence alone.
