import fs from 'node:fs/promises';
import path from 'node:path';
import {createRequire} from 'node:module';
import {fileURLToPath,pathToFileURL} from 'node:url';
import {createHash} from 'node:crypto';
const P=path.dirname(fileURLToPath(import.meta.url));const R='D:/VSCode Projects/cafecorner_nodebench/nodebench_ai4/.portfolio-worktrees/nodeslide-current-consumer-20260905';
const req=createRequire(path.join(R,'package.json'));const {chromium}=req('playwright');const {expect}=req('playwright/test');
const parentPrivate='D:/ProjectRecovery/NodeSlide first run 20260905/private-bootstrap-02';
const privateRoot=path.join(parentPrivate,'browser-responsive-02');const O=path.join(P,'E6g-nodeslide-responsive-first-use-02');await fs.mkdir(O);await fs.mkdir(privateRoot);
const {url}=JSON.parse(await fs.readFile(path.join(parentPrivate,'browser-first-use-01/deck-location.json'),'utf8'));const origin=new URL(url).origin;
const storage=JSON.parse(await fs.readFile(path.join(parentPrivate,'browser-notes-export-01/storage-state.json'),'utf8'));
const secrets=[];for(const o of storage.origins)for(const x of o.localStorage){if(x.name.includes('sessionId'))secrets.push(x.value);if(x.name==='nodeslide.deckAccess.v1')secrets.push(...Object.values(JSON.parse(x.value)));}
const safe=s=>secrets.reduce((a,b)=>b?a.replaceAll(b,'[PRIVATE_SESSION]'):a,String(s));
const report={at:new Date().toISOString(),proof:'NODESLIDE_CURRENT_DETERMINISTIC_FIRST_RUN_01',cells:[],errors:[],blockedExternal:[],fullGrades:null,resizeProtocol:'One exact viewport per new context; DOM, console and pixels captured before closing it. Text enlargement is computed font sizing, not OS zoom.'};
const browser=await chromium.launch();
async function makeContext(viewport){const context=await browser.newContext({storageState:storage,viewport,acceptDownloads:true,serviceWorkers:'block'});await context.route('**/*',r=>{let u=new URL(r.request().url());if(['http:','https:'].includes(u.protocol)&&!['localhost','127.0.0.1'].includes(u.hostname)){report.blockedExternal.push(u.origin+u.pathname);return r.abort();}return r.continue();});return context;}
async function settledEditor(page){await page.getByTestId('slide-canvas').waitFor();await expect(page.getByTestId('deck-title')).toHaveValue('NodeSlide local handoff — illustrative sourcing pilot');await expect(page.getByLabel(/^Presenter notes for /)).toHaveValue(/Illustrative pilot only/);await page.evaluate(()=>new Promise(resolve=>requestAnimationFrame(()=>requestAnimationFrame(resolve))));}
async function capture(page,name){await page.evaluate(()=>document.fonts.ready);await page.screenshot({path:path.join(O,name+'.png')});await fs.writeFile(path.join(privateRoot,name+'.html'),await page.content());const dom=await page.evaluate(()=>({text:document.body.innerText,viewport:{width:innerWidth,height:innerHeight},overflow:document.documentElement.scrollWidth-innerWidth,focused:{tag:document.activeElement?.tagName,label:document.activeElement?.getAttribute('aria-label')},controls:[...document.querySelectorAll('button,input,textarea,select,[role="tab"]')].map(e=>({tag:e.tagName,testid:e.getAttribute('data-testid'),label:e.getAttribute('aria-label'),text:e.textContent?.trim().slice(0,140),fontSize:getComputedStyle(e).fontSize,box:(()=>{const r=e.getBoundingClientRect();return{x:r.x,y:r.y,width:r.width,height:r.height};})()}))}));await fs.writeFile(path.join(O,name+'.json'),safe(JSON.stringify(dom,null,2))+'\n');return dom;}
try{
 const c=await makeContext({width:1440,height:960});const page=await c.newPage();await page.goto(url);await page.getByTestId('nodeslide-studio').waitFor();await settledEditor(page);await page.getByTestId('inspector-tab-json').click();const pending=page.waitForEvent('download');await page.getByRole('button',{name:'Download deck.json',exact:true}).click();const download=await pending;const file=path.join(privateRoot,'deck.json');await download.saveAs(file);const data=await fs.readFile(file);if(secrets.some(s=>s&&data.toString('utf8').includes(s)))throw new Error('JSON includes private session capability');const parsed=JSON.parse(data);await fs.copyFile(file,path.join(O,'deck.json'));report.jsonExport={bytes:data.length,sha256:createHash('sha256').update(data).digest('hex'),keys:Object.keys(parsed),notesPresent:data.toString('utf8').includes('Illustrative pilot only.'),downloadFailure:await download.failure()};await capture(page,'01-json-downloaded');await c.close();
 for(const [width,height] of [[360,800],[390,844],[768,1024],[1024,768],[1440,960],[1920,1080]]){
   const c=await makeContext({width,height});const page=await c.newPage();let errors=[];page.on('pageerror',e=>errors.push(safe(e.message)));page.on('console',m=>{if(m.type()==='error')errors.push(safe(m.text()));});
   for(const surface of ['landing','editor']){await page.goto(surface==='landing'?origin:url,{waitUntil:'domcontentloaded'});await page.getByTestId(surface==='landing'?'nodeslide-landing':'nodeslide-studio').waitFor();if(surface==='editor')await settledEditor(page);const name=`${surface}-${width}x${height}`;const dom=await capture(page,name);report.cells.push({name,viewport:{width,height},textPercent:100,overflow:dom.overflow,errors:[...errors]});}
   await c.close();
 }
 for(const [width,height] of [[390,844],[1440,960]]){
   const c=await makeContext({width,height});const page=await c.newPage();let errors=[];page.on('pageerror',e=>errors.push(safe(e.message)));await page.goto(url,{waitUntil:'domcontentloaded'});await page.getByTestId('nodeslide-studio').waitFor();await settledEditor(page);await page.evaluate(()=>{const values=[...document.querySelectorAll('body,body *')].map(e=>[e,parseFloat(getComputedStyle(e).fontSize)]);for(const [e,size]of values)if(Number.isFinite(size)&&e instanceof HTMLElement)e.style.fontSize=`${size*2}px`;});const notes=page.getByLabel(/^Presenter notes for /);const goal=await notes.isVisible();let reached=false,tabs=0;if(goal){await page.keyboard.press('Tab');for(;tabs<160;tabs++){if(await notes.evaluate(e=>e===document.activeElement)){reached=true;break;}await page.keyboard.press('Tab');}}
   const name=`editor-${width}x${height}-text200`;const dom=await capture(page,name);let focus=null;if(reached)focus=await notes.evaluate(e=>{const r=e.getBoundingClientRect();const p=document.elementFromPoint(r.x+r.width/2,Math.min(innerHeight-1,r.y+r.height/2));return{box:{x:r.x,y:r.y,width:r.width,height:r.height},centerHitIsNotes:p===e||e.contains(p),withinViewport:r.x>=0&&r.y>=0&&r.right<=innerWidth&&r.bottom<=innerHeight};});report.cells.push({name,viewport:{width,height},textPercent:200,overflow:dom.overflow,notesVisible:goal,notesReachedByNativeTab:reached,tabCount:tabs,focus,errors});await c.close();
 }
 const htmlContext=await makeContext({width:1440,height:960});const htmlPage=await htmlContext.newPage();await htmlPage.goto(pathToFileURL(path.join(parentPrivate,'browser-notes-export-01/actual.html')).href);await htmlPage.waitForTimeout(400);await capture(htmlPage,'exported-html-opened');report.htmlOpened={title:await htmlPage.title(),bodyTextLength:(await htmlPage.locator('body').innerText()).length};await htmlContext.close();
 report.status='RESPONSIVE_AND_ACTUAL_JSON_HTML_OBSERVED';
}catch(e){report.status='FAILED';report.failure=safe(e.stack??e);process.exitCode=1;}
finally{await browser.close();await fs.writeFile(path.join(O,'report.json'),JSON.stringify(report,null,2)+'\n');console.log(JSON.stringify(report));}
