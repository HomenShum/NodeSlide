# NodeSlide notes and controls: scoped handoff

A presenter must be able to edit what they will say, navigate slides and adjust zoom without one control covering another. Read [HANDOFF](../../../HANDOFF.md) for fresh setup and the remaining product limits. This packet preserves the exact reviewed layout repair and actual operator evidence; it is not a new provider, production or native Office claim.

| Decision | Evidence |
| --- | --- |
| Which source and scope were accepted? | [Independent source judgment](raw/E6g_NODESLIDE_NOTES_CONTROLS_FINAL_JUDGE.md.inert.txt), [source/artifact freeze](raw/E6g_NODESLIDE_NOTES_CONTROLS_FREEZE.json), [165 final custody checks](raw/E6g-nodeslide-notes-final-independent/final-guard.json) |
| Was the overlap actually observed? | [Current before report](raw/E6g-nodeslide-notes-before-01/report.json), [phone before](raw/E6g-nodeslide-notes-before-01/390-100-open-boundary.png), [phone after](raw/E6g-nodeslide-notes-after-02/390-100-open-boundary.png), [eleven after geometry states](raw/E6g-nodeslide-notes-after-02/report.json) |
| Do real controls and saved notes work? | [Native action report, including its final failed export](raw/E6g-nodeslide-notes-interactions-01/report.json), [recovery report](raw/E6g-nodeslide-notes-export-recovery-01/report.json), [settled recovery pixels](raw/E6g-nodeslide-notes-export-recovery-01/1440-recovered-export.png) |
| What file actually resulted? | [Recovered JSON](raw/E6g-nodeslide-notes-export-recovery-01/recovered.json), [recovered PPTX](raw/E6g-nodeslide-notes-export-recovery-01/recovered.pptx), [independent ZIP/XML/note inspection](raw/E6g-nodeslide-notes-export-independent/report.json) |
| Did ordinary checks pass? | [Current command](raw/E6g-nodeslide-notes-check-01/command.json) and [unmodified normal check log](raw/E6g-nodeslide-notes-check-01/normal-check.log) |

The accepted three-owner change moves existing pager and zoom groups into one normal-flow wrapping row before notes. The Fit height correction reserves content space. Handlers, note save keys, slide-count/zoom behavior, state, dependencies and provider/export policy remain unchanged. Independent source/geometry review passes 28 checks; action/export review passes 31, followed by 165 final byte checks. The source judge inspected preserved evidence and did not independently rerun the browser or suite.

Ten genuine before states preserve the overlap; eleven after states record separate rows and unobstructed target hits. Twelve consecutive note revisions and three native action bundles preserve blur/save, navigation, toggle, zoom and reload. The original script correctly ends FAILED when its replacement note removed required illustrative disclosure and PPTX export was refused. Recovery restores disclosure plus the new note and advances the deck from version 15 to 16. Five actual files remain: three original files unchanged and two recovery outputs. Both PPTX ZIPs pass CRC and all 40 XML/relationship parts parse; the first slide's exact note precedes appended provenance. This is not native Office rendering.

The current ordinary check passed in 150.969 seconds: lint, app/workspace typechecks, 2,823 root tests with seven existing skips, 42 workspace tests and builds. The earlier Windows NodeBook timeout remains unexplained. No timeout, retry, skip, concurrency or dependency setting changed. Large-chunk advisories remain in the log.

## Verify the portable bytes

From the repository root, using Python 3 and Git:

```powershell
python promotion/evidence/notes-controls-20260905/verify.py
```

The verifier uses only the standard library and Git. It checks raw payload SHA-256/Git blobs and the normalized current Git identities of the three source files plus HANDOFF. It does not install, run archived scripts, contact a service or load private state. A later source revision can intentionally fail current source binding while all historical evidence remains valid. The manifest excludes its own self-hash; the publication freeze is an external receipt.

All 151 frozen operator files (8,878,149 bytes) and the final independent raw directories/reports are byte-preserved. [The raw-copy map](raw-copy-map.json) retains original names. Historical code, CSS, HTML, Markdown and special attributes are inert `.txt` records so current collectors do not execute or reinterpret them. This packet's `.gitattributes` disables text conversion. Private bootstrap/environment, access capabilities, full authenticated browser HTML, databases and session material remain excluded; none of the 151 listed operator files is omitted.

## Limits and historical identity

Full grades remain null. Enlarged header/slide/inspector clipping, textarea resize and short-height behavior, sustained/multi-user load, accessibility/platform/device/browser zoom, provider and production remain unproved. Computed-font doubling resets on reload. Twelve rounds are not a one-minute endurance test. The loading inspector in after02 is not settled parity; recovery pixels show one actual settled state. The missing before-stage selector, Fit failure and hash-matched reconstructed stage1 CSS remain distinguished from genuine captures. Reader-format and newline-description corrections are retained.

[The dated PR 185/base judgment](raw/E6g_NODESLIDE_POSTMERGE_FINAL_JUDGE.md.inert.txt) records merged `8177142` and its public read on 2026-09-05. That is historical base identity, not publication of the current notes fix. This packet awaits publication review and normal shared integration. The older portfolio packet is unchanged.
