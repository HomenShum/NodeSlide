Builder. **Re your request:** execute individual repository readiness and handoff. Root accepts the bounded NODESLIDE_CURRENT_DETERMINISTIC_FIRST_RUN_01 plan at exact canonicalb0674b229a3691c09c88fcab1481f2e33342cb21, treef3262e171240fec8f54ec5f01aa86310a2b30f5c. The read-only recon JSON is frozen at SHA256f6c2d3aacba37f4f950480fe622f49443b0012ff4dbbcf5b0e08bf862fb6e20f.

Root owns this separate first-run alongside the sole NodeRoom implementation worker. Create only the proposed sibling candidate/branch, preserve both existing clones and their dirty work, install the lock, inspect command requirements and run the documented local checks and packed external-agent job. Bind commands and source bytes. No source repair or shared merge is part of this initial proof.

For browser work, explicitly use a new isolated local Convex backend. Inspect the installed CLI's current options first; the generic historical `convex dev --once` instruction must not be allowed to choose a shared cloud deployment. Set creation admission only after binding the new local deployment. Keep all generated local credentials private. Choose deterministic mode before creating the synthetic deck, allocate an owned unused port and explicitly set the browser base URL. Protected4173/4174 and all original environments remain untouched.

Inspect actual deck edit/reload/JSON/PPTX results without native Office and capture390/1440 pixels, keyboard and enlarged text. No provider, cloud deployment, native COM, historical worktree cleanup or benchmark score is authorized here. Missing local readiness is recorded, not replaced by fixtures. Any observed repair needs its own root-cause plan and actual before boundary.
