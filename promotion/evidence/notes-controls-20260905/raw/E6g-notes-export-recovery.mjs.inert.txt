import fs from 'node:fs/promises';import path from 'node:path';import {createRequire}from'node:module';import{fileURLToPath}from'node:url';import{createHash}from'node:crypto';
const P=path.dirname(fileURLToPath(import.meta.url));const R='D:/VSCode Projects/cafecorner_nodebench/nodebench_ai4/.portfolio-worktrees/nodeslide-current-consumer-20260905';const req=createRequire(path.join(R,'package.json'));const{chromium}=req('playwright');const{expect}=req('playwright/test');const JSZip=req('jszip');
const prior=JSON.parse(await fs.readFile(path.join(P,'E6g-nodeslide-notes-interactions-01/report.json'),'utf8'));expect(prior.steps).toHaveLength(15);expect(prior.files).toHaveLength(3);expect(prior.status).toBe('FAILED');
const base='D:/ProjectRecovery/NodeSlide first run 20260905/private-bootstrap-02';const{url}=JSON.parse(await fs.readFile(path.join(base,'browser-first-use-01/deck-location.json'),'utf8'));const storage=JSON.parse(await fs.readFile(path.join(base,'browser-first-use-01/storage-state.json'),'utf8'));
const secrets=[];for(const origin of storage.origins)for(const item of origin.localStorage){if(item.name.includes('sessionId'))secrets.push(item.value);if(item.name==='nodeslide.deckAccess.v1')secrets.push(...Object.values(JSON.parse(item.value)));}
const safe=s=>secrets.reduce((v,k)=>k?v.replaceAll(k,'[PRIVATE_SESSION]'):v,String(s));const hash=b=>createHash('sha256').update(b).digest('hex');
const O=path.join(P,'E6g-nodeslide-notes-export-recovery-01');const priv=path.join(base,'notes-export-recovery-01');await fs.mkdir(O);await fs.mkdir(priv);
const report={proof:'NODESLIDE-NOTES-CONTROLS-01',startedAt:new Date().toISOString(),reason:'Prior test notes removed the required illustrative disclosure. Actual export was refused; restore disclosed original note plus latest new note without changing product/gate or rerunning passed interactions.',files:[],errors:[],blockedOrigins:[],priorFilesExact:false,fullGrades:null};
const browser=await chromium.launch();const context=await browser.newContext({storageState:storage,viewport:{width:1440,height:960},acceptDownloads:true,serviceWorkers:'block'});await context.route('**/*',r=>{const u=new URL(r.request().url());if(['http:','https:'].includes(u.protocol)&&u.hostname!=='127.0.0.1'){report.blockedOrigins.push(u.origin);return r.abort();}return r.continue();});const page=await context.newPage();page.on('pageerror',e=>report.errors.push(safe(e.message)));page.on('console',m=>{if(m.type()==='error')report.errors.push(safe(m.text()));});
try{
 await page.goto(url,{waitUntil:'domcontentloaded'});await page.getByTestId('slide-canvas').waitFor();const notes=page.getByLabel(/^Presenter notes for /);await expect(notes).toHaveValue(prior.files[2].expectedNote);const version=()=>page.locator('.ns-version-label').textContent().then(s=>Number(s.match(/v(\d+)/)?.[1]));const oldVersion=await version();
 const restored=prior.files[0].expectedNote+'\n'+prior.files[2].expectedNote;report.restoredNote=restored;await notes.fill(restored);await notes.press('Tab');await expect.poll(version).toBe(oldVersion+1);await page.reload({waitUntil:'domcontentloaded'});await page.getByTestId('slide-canvas').waitFor();await expect(notes).toHaveValue(restored);report.versions={before:oldVersion,after:await version()};
 for(const format of ['json','pptx']){
  if(format==='json')await page.getByTestId('inspector-tab-json').click();else{await page.getByLabel('Export deck',{exact:true}).click();await page.getByTestId('export-pptx').waitFor();}
  const pending=page.waitForEvent('download',{timeout:15000});await(format==='json'?page.getByRole('button',{name:'Download deck.json',exact:true}):page.getByTestId('export-pptx')).click();const download=await pending;const file=path.join(priv,'recovered.'+format);await download.saveAs(file);const b=await fs.readFile(file);let content;
  if(format==='json'){content=b.toString('utf8');expect(JSON.parse(content).slides[0].notes).toBe(restored);}else{const z=await JSZip.loadAsync(b);content=(await Promise.all(Object.keys(z.files).filter(n=>n.endsWith('.xml')&&!z.files[n].dir).map(n=>z.file(n).async('string')))).join('\n');expect(content).toContain('Illustrative pilot only.');expect(content).toContain('Notes handoff 320 round 4: 00123 stays text; 中文交接.');}
  expect(secrets.some(s=>s&&content.includes(s))).toBe(false);expect(await download.failure()).toBeNull();await fs.copyFile(file,path.join(O,'recovered.'+format));report.files.push({path:'recovered.'+format,bytes:b.length,sha256:hash(b)});
  if(format==='json')await page.getByTestId('inspector-tab-ai').click();
 }
 await page.evaluate(()=>document.fonts.ready);await page.screenshot({path:path.join(O,'1440-recovered-export.png')});await fs.writeFile(path.join(priv,'1440-recovered-export.html'),await page.content());await fs.writeFile(path.join(O,'1440-recovered-export.json'),safe(JSON.stringify({text:await page.locator('body').innerText(),notes:await notes.inputValue(),version:await version()},null,2))+'\n');
 for(const f of prior.files)expect(hash(await fs.readFile(path.join(P,'E6g-nodeslide-notes-interactions-01',f.path)))).toBe(f.sha256);report.priorFilesExact=true;
 for(const f of prior.source)expect(hash(await fs.readFile(path.join(R,f.path)))).toBe(f.sha256);report.sourceUnchanged=true;expect(report.errors).toEqual([]);expect(report.blockedOrigins).toEqual([]);report.status='PASS_DISCLOSURE_REPAIR_AND_ACTUAL_EXPORT_RECOVERY';
}catch(e){report.status='FAILED';report.failure=safe(e.stack??e);await page.screenshot({path:path.join(O,'failure.png')}).catch(()=>{});process.exitCode=1;}
finally{await context.close();await browser.close();report.finishedAt=new Date().toISOString();await fs.writeFile(path.join(O,'report.json'),safe(JSON.stringify(report,null,2))+'\n');console.log(JSON.stringify({status:report.status,files:report.files.length,errors:report.errors.length,versions:report.versions,failure:report.failure??null}));}
