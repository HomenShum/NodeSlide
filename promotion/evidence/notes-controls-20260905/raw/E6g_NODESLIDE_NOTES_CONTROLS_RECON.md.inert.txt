# Notes and canvas controls: bounded repair proposal

Mode: Builder. **Allocate one normal-flow row for slide navigation and zoom, followed by the notes strip.** A presenter must be able to read and edit the last note lines and move to the next slide without clicking through another control. The current layout fails that job because the controls float over an area the notes also occupy.

**Re your request:** make each individual repository ready for usage and handoff, including responsive interaction standards. This is independent source/current-before diagnosis for `NODESLIDE-NOTES-CONTROLS-01`, not an implementation or grade.

The ten fresh cells bind merged `8177142416b6a6f59df057d0e5ab25df08db93ec` and three exact source owners. I directly viewed the labelled 390-normal, 390-text200 and 1440-normal captures. Both control groups cover the notes area. The 320-normal and 390-text200 records show the visible enabled Next button failing its center hit test; collapsed 390/1440 records show the notes toggle failing it. Hidden pan controls have zero-size boxes by existing CSS and are excluded.

The upstream owner is the canvas panel layout. `SlideCanvas.tsx:605` renders pager, `:632` notes and `:635` zoom as siblings. `nodeslide.css:926` gives the panel a positioned grid; `:1252` positions both control groups absolutely. The notes at `:1627` remain in flow. `nodeslideV3.css:1926/1931` anchors the controls at the panel bottom, which includes the notes. Its right offset also leaves zoom's inherited `left:50%` and translation active; at `:5201` the phone skin restores the pager, allowing the two groups to collide.

Use the existing three owners only: a plain wrapper around pager and zoom directly below the viewport, then notes. The wrapper lays out its two existing groups with flex wrapping. The panel reserves explicit content-driven control/notes rows while preserving the shrinking scrollable viewport. Replace the conflicting absolute-position/inset/transform declarations at their current owners. Let enlarged labels and groups choose their height; preserve current keyboard/click handlers, slide identity, disabled boundaries, zoom range/Fit, notes keys and exact save-on-blur semantics. No measured heights, guessed offsets, new state or stacking workaround is needed.

Before judging the repair, replay the same ten cells and original blocked-Next cases with actual keyboard/pointer input. Verify notes and control rectangles do not intersect, visible enabled controls receive hits, enlarged text remains inside its owned boxes, multiline Unicode notes survive blur/navigation/reload, and burst plus one-minute repeated editing/navigation keeps the correct current slide and values. Cover textarea resize, canvas scroll and no-notes/read-only layout; preserve neighboring surfaces outside the declared row-height effect. The existing `.ns-slide-stage` measurement is missing: use actual `.ns-canvas-stage` in future proof, retain the original missing result. No full grade follows from this slice.

No source, configuration, index, refs, runtime or retained evidence changed. [Structured plan, exact bindings and ten-cell overlap measurements](E6g_NODESLIDE_NOTES_CONTROLS_RECON.json), [fresh before report](E6g-nodeslide-notes-before-01/report.json), [390 enlarged boundary](E6g-nodeslide-notes-before-01/390-200-open-boundary.png), [1440 boundary](E6g-nodeslide-notes-before-01/1440-100-open-boundary.png).
