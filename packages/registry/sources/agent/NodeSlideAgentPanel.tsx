import type { DeckAgentThreadProps } from '@nodeslide/react';
import { DeckAgentThread } from '@nodeslide/react';

export function NodeSlideAgentPanel(props: DeckAgentThreadProps) {
  return <DeckAgentThread {...props} />;
}
