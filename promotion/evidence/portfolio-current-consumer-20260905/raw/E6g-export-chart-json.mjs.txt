import fs from 'node:fs/promises';import path from 'node:path';import{createRequire}from'node:module';import{fileURLToPath}from'node:url';import{createHash}from'node:crypto';
const P=path.dirname(fileURLToPath(import.meta.url));const req=createRequire('D:/VSCode Projects/cafecorner_nodebench/nodebench_ai4/.portfolio-worktrees/nodeslide-current-consumer-20260905/package.json');const{chromium}=req('playwright');
const privateRoot='D:/ProjectRecovery/NodeSlide first run 20260905/private-bootstrap-02/browser-chart-fixture-01';const O=path.join(P,'E6g-nodeslide-chart-json-01');await fs.mkdir(O);
const{url}=JSON.parse(await fs.readFile(path.join(privateRoot,'deck-location.json'),'utf8'));const storage=JSON.parse(await fs.readFile(path.join(privateRoot,'storage-state.json'),'utf8'));
const secrets=storage.origins.flatMap(o=>o.localStorage.flatMap(x=>x.name==='nodeslide.deckAccess.v1'?Object.values(JSON.parse(x.value)):x.name.includes('sessionId')?[x.value]:[]));
const browser=await chromium.launch();const context=await browser.newContext({storageState:storage,viewport:{width:1440,height:960},acceptDownloads:true,serviceWorkers:'block'});
try{await context.route('**/*',r=>{let u=new URL(r.request().url());return['http:','https:'].includes(u.protocol)&&u.hostname!=='127.0.0.1'?r.abort():r.continue();});const page=await context.newPage();await page.goto(url);await page.getByTestId('slide-canvas').waitFor();await page.getByTestId('inspector-tab-json').click();const pending=page.waitForEvent('download');await page.getByRole('button',{name:'Download deck.json',exact:true}).click();const download=await pending;const file=path.join(privateRoot,'actual-deck.json');await download.saveAs(file);const bytes=await fs.readFile(file);const text=bytes.toString('utf8');if(secrets.some(s=>s&&text.includes(s)))throw new Error('Private capability present');await fs.copyFile(file,path.join(O,'deck.json'));const d=JSON.parse(text);const report={bytes:bytes.length,sha256:createHash('sha256').update(bytes).digest('hex'),slideCount:d.slides.length,elementTypes:d.elements.reduce((a,e)=>(a[e.type]=(a[e.type]??0)+1,a),{}),downloadFailure:await download.failure(),providerCalls:0};await fs.writeFile(path.join(O,'report.json'),JSON.stringify(report,null,2)+'\n');console.log(JSON.stringify(report));}finally{await context.close();await browser.close();}
