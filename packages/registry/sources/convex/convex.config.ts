/**
 * Host-owned mount pointer. Import this from the app's root convex.config.ts
 * and pass it to app.use(); the component package owns its isolated schema.
 */
export { default } from '@nodeslide/convex/convex.config.js';
