from pathlib import Path
import hashlib, json, subprocess, datetime
P=Path(__file__).resolve().parent
O=P/'E6g-nodeslide-check-diagnosis'
R=Path('D:/VSCode Projects/cafecorner_nodebench/nodebench_ai4/.portfolio-worktrees/nodeslide-current-consumer-20260905')
def h(b): return hashlib.sha256(b).hexdigest()
def load(name): return json.loads((O/name).read_text(encoding='utf-8-sig'))
def save(path,value): path.write_text(json.dumps(value,indent=2,ensure_ascii=True)+'\n',encoding='utf-8')
def git(*a): return subprocess.check_output(['git','--no-optional-locks',*a],cwd=R)
before=load('source-before.json')
extra=['.gitattributes','.tours/01-primary-user-flow.tour','.tours/02-agent-execution.tour','.tours/03-debug-and-recovery.tour']
extra_bindings={}
for name in extra:
    b=(R/name).read_bytes(); target=O/'source-before'/name
    target.parent.mkdir(parents=True,exist_ok=True); target.write_bytes(b)
    extra_bindings[name]={'sha256':h(b),'bytes':len(b),'headBlobSha256':h(git('show',f'HEAD:{name}'))}
checks={p: h((R/p).read_bytes())==v['sha256'] for p,v in before['files'].items()}
tour=load('tour-line-ending-causality.json')
tour_input_checks={p:h((R/p).read_bytes())==v['sha256'] for p,v in tour['bindings'].items()}
custody={'sourceAndInstalledInputs':len(checks),'allUnchanged':all(checks.values()),'changed':[p for p,ok in checks.items() if not ok], 'generatorReadInputs':len(tour_input_checks),'allGeneratorReadInputsUnchanged':all(tour_input_checks.values()),'indexUnchanged':git('ls-files','--stage','-z').hex()==before['indexTree'],'refsUnchanged':git('show-ref').decode()==before['refs'],'head':git('rev-parse','HEAD').decode().strip(),'extraBindings':extra_bindings,'boundary':'Only named source, tours, installed NodeBook files, Git entries and refs. Root concurrently owns ordinary generated dist build; no all-filesystem immutability claim.'}
assert custody['allUnchanged'] and custody['allGeneratorReadInputsUnchanged'] and custody['indexUnchanged'] and custody['refsUnchanged']
save(O/'final-custody.json',custody)
for name in ['E6g-diagnose-nodeslide-check.py','E6g-nodeslide-check-path-probe.mjs','E6g-nodeslide-tour-eol-probe.mjs']:
    (O/name).write_bytes((P/name).read_bytes())
manifest={str(f.relative_to(O)).replace('\\','/'):{'sha256':h(f.read_bytes()),'bytes':f.stat().st_size} for f in O.rglob('*') if f.is_file() and f.name!='manifest.json'}
save(O/'manifest.json',{'schemaVersion':1,'root':str(O),'files':manifest,'count':len(manifest)})
causal=load('path-and-discovery-causality.json')
report={
 'mode':'Builder / independent read-only diagnosis',
 'verdict':'CONFIRMED_NARROW_PORTABILITY_CAUSES_NODEBOOK_FULL_SUITE_TIMEOUT_UNRESOLVED',
 'request':'Diagnose the initial NodeSlide check failures before source repair; preserve exact failures and propose the smallest truthful first-run repair.',
 'repo':str(R),'head':custody['head'],'tree':git('rev-parse','HEAD^{tree}').decode().strip(),
 'originalCheck':{'exitCode':1,'failed':4,'passed':2808,'skipped':7,'buildReached':False,'workspaceTestsReached':False,'lintAndTypecheckPassed':True,'raw':'E6g-nodeslide-check-diagnosis/original-full-check.log'},
 'unchangedTargeted':{'exitCode':1,'failed':3,'passed':5,'skipped':0,'files':3,'sixKindsDurationMs':1394.1797,'commandReceipt':'E6g-nodeslide-check-diagnosis/unchanged-targeted-command.json'},
 'causes':[
  {'id':'NODESLIDE_TEST_FILE_URL_01','status':'CONFIRMED_NOT_EDITED','owners':['scripts/tests/workspace-build-depth.test.mjs:28','scripts/tests/code-tours.test.mjs:15'],'symptom':'Two workspace assertions and one child ENOENT fail before their real checks.','boundary':'URL pathname is used as filesystem cwd/root; literal %20 points to absent directory.','mechanism':'Removing the leading slash on a drive letter does not decode URL escapes. Child executable exists, but spawnSync reports ENOENT because cwd does not exist. readManifest catches the same ENOENT and returns null, making the second build assertion compare an empty string.','knockout':'Bad cwd fails to spawn; fileURLToPath cwd runs the unchanged generator. Bad manifest root fails; correct root reads 15 manifests, no nested npm offenders, both Convex outputs retained.','repair':'Import fileURLToPath from node:url in exactly both test owners and use fileURLToPath(new URL(\'../..\', import.meta.url)).','residual':'Correct cwd exposes a separate tour line-ending failure; cwd change alone is not a green tours gate.'},
  {'id':'NODESLIDE_TOUR_EOL_01','status':'CONFIRMED_NOT_EDITED','owners':['.gitattributes','scripts/build-code-tours.mjs:232'],'symptom':'The genuinely executed --check flags all three untouched tours.','boundary':'Generator expects exact LF JSON; Git checks .tour files out CRLF under core.autocrlf=true because their extension is absent from eol=lf attributes.','knockout':'Exact generator body under read-only filesystem facade: real CRLF fails three; virtual CRLF-to-LF only passes 3 tours / 32 steps; one wrong line after normalization fails one. All three normalized files equal actual HEAD blobs.','repair':'Add *.tour text eol=lf at the existing Git text policy and normalize only the three current .tour working bytes after preserving originals. No tour semantic change or weakening of generator exact comparison.','residual':'New checkout with normal Git filtering must prove this; no candidate attribute change was made here.'},
  {'id':'NODESLIDE_TRACE_DISCOVERY_01','status':'CONFIRMED_DIAGNOSTIC_SEAM_NOT_ONE_OF_FOUR_FAILURES','owners':['scripts/tests/playwright-trace.test.mjs:17'],'symptom':'Two FIND diagnostics are caught and represented as no real traces.','boundary':'Unix filesystem find command resolves to Windows FIND text utility; command error is swallowed into [] before describe.runIf.','knockout':'Actual shell exits 2 with FIND parameter error; candidate .tmp is actually absent, so five real-trace tests currently have a legitimate absence condition independently. Native filesystem discovery returns no actual traces, finds exactly two nested diagnostic names with spaces/#, and propagates ENOTDIR. Diagnostic zip names are explicitly not real traces or parser acceptance.','repair':'Replace shell discovery with Node fs traversal, preserving no-traces skip only for genuine absence; propagate other errors. Sort discovered real paths for deterministic selection. Do not fabricate traces or force real-trace cases to pass.','residual':'No actual real trace exists in this candidate. Existing five real-trace cases remain NOT_RUN until one is captured by the authorized app proof.'},
  {'id':'NODESLIDE_NODEBOOK_TIMEOUT_01','status':'UNRESOLVED_FULL_SUITE_ONLY_NOT_REPRODUCED_IN_BOUNDED_REPLAY','owners':['src/domains/nodeslide/components/NodeBookWorkspacePanel.test.tsx:167'],'symptom':'Original six-kind test reached 68030 ms and failed existing 60000 ms test limit.','boundary':'The test includes Promise.all plugin import/validation and then six sequential real host mounts with 15000 ms waits. The initial log gives no per-stage event, so it does not identify loading, validation or rendering as the failing phase.','observation':'All five unchanged tests pass with exact installed NodeBook inputs; all six kinds render in 1394.1797 ms. Same test limits, mocks and default Vitest config.','inference':'Full-suite transform/import/worker contention or cold-cache timing is plausible; not causally established by this smaller replay.','repair':'No NodeBook runtime/test timeout/skip change. After the mechanical portability repair, run the normal original check once. If the same timeout returns, retain it and collect test-stage timings plus worker/event-loop evidence before choosing a scheduling or loader change.','residual':'Bounded success does not certify whole-suite reliability, actual browser visual fidelity or performance.'}
 ],
 'plan':{'name':'NODESLIDE_CHECK_PORTABILITY_01','classification':'Patch / developer first-run tests and Git line-ending policy','authoredPaths':['scripts/tests/workspace-build-depth.test.mjs','scripts/tests/code-tours.test.mjs','.gitattributes','scripts/tests/playwright-trace.test.mjs'],'steps':['Preserve failed run and exact current four owners plus CRLF tour bytes before any edit.','Use native URL-to-path conversion in the two actual failing test owners.','Add the existing LF rule for .tour and normalize only those working files; assert their Git blobs and parsed JSON remain unchanged.','Replace Unix find with native filesystem discovery; assert nested paths with spaces, actual missing .tmp and non-absence error behavior.','Replay unchanged workspace/tours assertions and the existing trace tests; assert real-trace skip remains explicitly NOT_RUN when absent and no catch hides errors.','Run original npm run check with unchanged timeouts/skips, after avoiding concurrent builds on the same generated outputs.','Keep NodeBook failure unresolved if it recurs, instrument the exact six-kind phase without relaxing its assertions, then freeze an independent narrow review.'], 'notInScope':['NodeBook component redesign','timeout increase','disabling tests or assertions','whole-app readiness grade','provider/cloud/native Office calls','native Office fidelity','historical worktree cleanup']},
 'twinSearch':'TWINS: searched scripts/tests for .pathname and pathname.replace - the two named owners are the only matches; existing docs-citations and nodeslide-landing-a11y tests already use fileURLToPath. The Unix find site is a distinct observed discovery defect.',
 'custody':custody,'evidenceManifest':{'path':'E6g-nodeslide-check-diagnosis/manifest.json','sha256':h((O/'manifest.json').read_bytes())},
 'limitations':['No source/config/index/ref edits, backend/provider/native Office or full check rerun by this reviewer.','Review context reused from earlier unrelated repairs; nonauthor of this NodeSlide source.','No browser pixels or whole visual/responsive/interaction grade claimed.','Original 4 failures and all new failures remain raw and unmodified.'],
 'overallScore':None,'dimensionScores':None,
}
save(P/'E6g_NODESLIDE_CHECK_DIAGNOSIS.json',report)
md='''Builder. **Three check failures have a confirmed filesystem-path cause; a second line-ending defect was hidden behind it. Keep the NodeBook timeout open at full-suite scope.** A developer cloning this project into a Windows folder containing spaces cannot reach the intended build and tour checks. The source below identifies the exact conversion and checkout boundaries; no product repair was made.

**Re your request:** make each repository usable and ready for developer handoff using planner, worker and independent judge — this is the causal plan required before repairing NodeSlide's first local check.

The candidate is `b0674b229a3691c09c88fcab1481f2e33342cb21`, tree `f3262e171240fec8f54ec5f01aa86310a2b30f5c`. The original normal check remains failed: 4 failures, 2,808 passes and 7 skips. Lint/typecheck passed; workspace tests and build were not reached. [Original raw log](E6g-nodeslide-check-diagnosis/original-full-check.log).

| Observed failure | Causal result | Smallest repair |
|---|---|---|
| Workspace manifest scan and missing Convex build string | `.pathname` keeps literal `%20`; no such filesystem directory exists. The helper catches the read failure and returns null. Correct native path reads all 15 workspaces, no nesting offenders, both Convex outputs. | Use `fileURLToPath` in `scripts/tests/workspace-build-depth.test.mjs:28`. |
| CodeTours child ENOENT | Node exists; the encoded child working directory does not. Correcting only cwd executes the real generator. | Same native conversion in `scripts/tests/code-tours.test.mjs:15`. |
| Real generator then flags three tours | `.tour` is missing from the existing LF policy. Git blobs are LF but Windows working files CRLF. Virtual LF-only comparison passes 3 tours / 32 steps; one wrong line still fails. | Add `*.tour text eol=lf`; preserve then normalize only three tour working files, with identical Git blobs and JSON. Keep exact generator comparison. |
| FIND diagnostics before tests | `scripts/tests/playwright-trace.test.mjs:19` invokes Unix `find`; Windows resolves a different utility. Its blanket catch hides the command failure as no evidence. Actual `.tmp` is absent here. | Native filesystem discovery, deterministic paths, genuine absence-only skip and other errors propagated. No fabricated real trace. |
| NodeBook six-kind 60-second timeout | All five unchanged NodeBook tests pass in the bounded replay; six real mounted kinds take 1,394 ms. The full-run log cannot locate its slow phase. | No timeout, skip, runtime or component change. Recheck the normal suite after path repairs; if reproduced, collect exact phase and worker timing first. |

[The unchanged targeted replay](E6g-nodeslide-check-diagnosis/unchanged-targeted.log) retained three failures and five passes with zero skips. It used the existing Vitest configuration, assertions, SVG/jsdom accommodations and limits. The [path knockout](E6g-nodeslide-check-diagnosis/path-and-discovery-causality.json) changes only the cwd expression outside the source. The [generator knockout](E6g-nodeslide-check-diagnosis/tour-line-ending-causality.json) executes the exact generator body in a read-only VM facade, changing only tour strings in memory. Diagnostic files named `trace.zip` test name discovery only; they are labelled non-trace data and never satisfy the real-trace parser tests.

The original full run spent 68,030 ms in the six-kind test. That test concurrently imports and validates plugins, then selects each kind and waits for its actual host SVG. Loading, validation, scheduling and rendering are not separately timed in the original log. Contention or cold transformations are plausible explanations, not established causes. The 1.4-second smaller replay does not close full-suite reliability or certify browser visuals. Preserve the original timeout; no blanket concurrency/config change follows from this evidence.

The proposed patch touches four existing owners: the two test files, `.gitattributes`, and the observed trace-discovery test. It introduces no dependencies, runtime APIs or configurable exceptions. Preserve current bytes; apply the native path and LF policy corrections; verify the same gates and wrong-line rejection; run the ordinary check with its existing limits. Any persistent NodeBook failure requires its own measured phase boundary before another change. The detailed ordered plan is in the [structured report](E6g_NODESLIDE_CHECK_DIAGNOSIS.json).

TWINS: searched scripts/tests for .pathname and pathname.replace - the two named owners are the only matches; existing docs-citations and nodeslide-landing-a11y tests already use fileURLToPath. The Unix find site is a distinct observed discovery defect.

[Custody](E6g-nodeslide-check-diagnosis/final-custody.json) verifies 61 source/installed NodeBook inputs, 20 generator-read inputs, index entries and refs unchanged. Root independently owns the normal generated build; this receipt deliberately makes no global generated-directory immutability claim. [Raw manifest](E6g-nodeslide-check-diagnosis/manifest.json) binds retained failures, probes and before files. No backend, provider, native Office, application source, config, index or ref changes were made by this reviewer. All final grades remain null.
'''
(P/'E6g_NODESLIDE_CHECK_DIAGNOSIS.md').write_text(md,encoding='utf-8')
print(json.dumps({'verdict':report['verdict'],'reportSha256':h((P/'E6g_NODESLIDE_CHECK_DIAGNOSIS.json').read_bytes()),'evidenceFiles':len(manifest),'custodyUnchanged':custody['allUnchanged']}))
